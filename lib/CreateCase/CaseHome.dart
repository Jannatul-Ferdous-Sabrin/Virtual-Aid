import 'package:flutter/material.dart';

class CaseHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Case Home',
          style: TextStyle(fontSize: 60),
        ),
      ),
    );
  }
}
