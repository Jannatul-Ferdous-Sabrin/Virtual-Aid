import 'package:flutter/material.dart';

class BottomSettingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Setting',
          style: TextStyle(fontSize: 60),
        ),
      ),
    );
  }
}
